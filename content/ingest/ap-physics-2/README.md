# AP Physics 2 — queued (not ingested yet)

Your Physics 2 books are still in **Downloads**. When ready, say **"do physics 2"**.

```bash
node scripts/ingest-licensed-pdf.mjs --track ap-physics-2 --from-downloads
```

Requires seed `026` (not created yet).
